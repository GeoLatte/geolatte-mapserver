The test TMS is the default TMS installed with TileCache (version 2.10).

The resources/tiles directory should be copied to '/tmp' and the file
ortho-jpeg.tgz in directory /tmp/tiles/orthos should be unzipped in place.

Useful system properties to specify from the command line:

-Dmapserver-configuration=./src/test/resources/test-reproj.xml
-Dlog4j.configuration=file:///<ROOT_DIR>/log4j.properties

